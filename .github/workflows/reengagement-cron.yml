name: Daily Re-engagement Emails

on:
  schedule:
    # Run every day at 8:00 AM UTC (1:30 PM IST)
    - cron: "0 8 * * *"
  workflow_dispatch: # allow manual trigger for testing

jobs:
  send-emails:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Trigger re-engagement email function
        run: |
          response=$(curl -s -o /tmp/resp.json -w "%{http_code}" \
            -X POST \
            "https://qanoiqzanywjrcuhsmny.supabase.co/functions/v1/send-reengagement-emails" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}')
          echo "HTTP status: $response"
          echo "Response: $(cat /tmp/resp.json)"
          if [ "$response" != "200" ]; then
            echo "Function returned non-200 status"
            exit 1
          fi
