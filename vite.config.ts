import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import path from "path";
import { componentTagger } from "lovable-tagger";

// https://vitejs.dev/config/
export default defineConfig(({ mode }) => ({
  server: {
    host: "::",
    port: 8080,
    hmr: {
      overlay: false,
    },
  },
  plugins: [react(), mode === "development" && componentTagger()].filter(Boolean),
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
    dedupe: ["react", "react-dom", "react/jsx-runtime", "react/jsx-dev-runtime"],
  },
  build: {
    // Let Vite handle chunk splitting automatically.
    // Route-level React.lazy() in App.tsx already gives us the performance win
    // (initial bundle ~119KB). Manual vendor splitting caused a React context
    // initialisation race (createContext called before React chunk loaded).
    chunkSizeWarningLimit: 800,
  },
}));
